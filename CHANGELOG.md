# Changelog

## Unreleased

- documented the current package/API contract
- added typed result contracts and dataframe validation helpers
- refactored shared OHLC and yfinance internals into reusable helper modules
- fixed installed-package config discovery to stop assuming a source checkout layout
- aligned Stooq daily volume typing with the canonical parquet schema
- added package metadata and `py.typed` for typed-distribution support
